[project]
name = "catiamcp"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "anthropic>=0.109.1",
    "httpx>=0.28.1",
    "mcp[cli]>=1.27.0",
    "ollama>=0.6.2",
    "pillow>=12.2.0",
    "python-dotenv>=1.2.2",
]

[tool.uv.workspace]
members = [
    "mcp-client",
]
